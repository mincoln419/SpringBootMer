const intitialState = {
    maincomment: []
};

const reducer = (state = intitialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
    
}

export default reducer;
